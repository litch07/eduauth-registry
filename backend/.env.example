# ============================================================================
# EduAuth Registry - Environment Configuration Example
# Copy this file to .env and update with your actual values
# ============================================================================

# ============================================================================
# 1. DATABASE CONFIGURATION
# ============================================================================
# MySQL database connection settings
# Make sure MySQL server is running before starting the application

DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=eduauth_registry
DB_PORT=3306

# ============================================================================
# 2. JWT CONFIGURATION
# ============================================================================
# JSON Web Token secret for authentication
# IMPORTANT: Change this to a random 32+ character string for production
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"

JWT_SECRET=your_super_secret_jwt_key_change_this_to_random_32_chars_minimum

# ============================================================================
# 3. APPLICATION CONFIGURATION
# ============================================================================
# Node.js environment and server settings

NODE_ENV=development
PORT=5000
FRONTEND_URL=http://localhost:3000

# ============================================================================
# 4. EMAIL CONFIGURATION (For future use)
# ============================================================================
# SMTP settings for sending notification emails
# Gmail: smtp.gmail.com, use App Password (not regular password)
# For Gmail: Enable 2FA and generate App Password from account settings

SMTP_SERVICE=gmail
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password-here

# ============================================================================
# Notes:
# - Never commit .env file to version control (add to .gitignore)
# - Use .env.example as a template for new developers
# - All database tables will be created automatically on first run
# - JWT_SECRET must be different for production environments
# - Email configuration is optional but required for email notifications
# ============================================================================
